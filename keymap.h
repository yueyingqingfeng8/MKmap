#ifndef KEYMAP_H
#define KEYMAP_H

#include <windows.h>

enum KeyMap
{
    _shift = VK_SHIFT,
    _ctrl  = VK_CONTROL,
    _alt   = VK_MENU,
    _space = VK_SPACE,
    _tab   = VK_TAB,

    _a = 65, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z

};

#endif // KEYMAP_H
